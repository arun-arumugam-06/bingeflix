{
  "name": "bingeflix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "render-build": "cd server && npm install && npm run render-build",
    "start": "cd server && npm run start"
  }
}
